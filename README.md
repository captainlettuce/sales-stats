# Quickstart
Assuming you have docker installed run
`docker compose up -d` in the repository root.

The api is reachable through "http://localhost:8080" by default and the only endpoint is "/aggregate". 
See `/api/api.yaml` openAPI-specification for an exact description.
